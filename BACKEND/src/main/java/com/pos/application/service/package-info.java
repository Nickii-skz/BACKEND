/**
 * Application services (use case implementations): ProductService, SaleService,
 * CategoryService, OrderService, DiscountService.
 *
 * <p>These classes implement input ports and invoke output ports.
 * They may use Spring annotations (@Service, @Transactional) but must NOT
 * import JPA, HTTP, or any other infrastructure concern.
 */
package com.pos.application.service;

/**
 * Output ports (repository/adapter interfaces): ProductRepository, SaleRepository,
 * CategoryRepository, DiscountRepository, InventoryPort, UserRepository.
 *
 * <p>Implemented by infrastructure JPA adapters. Invoked by application services.
 * No Spring, JPA, or infrastructure dependencies allowed in this package.
 */
package com.pos.application.port.output;

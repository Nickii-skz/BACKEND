/**
 * MapStruct mappers: ProductMapper, SaleMapper, CategoryMapper, DiscountMapper.
 *
 * <p>These interfaces are processed at compile time by the MapStruct annotation processor
 * to generate type-safe mapping implementations between domain models and JPA entities / DTOs.
 */
package com.pos.infrastructure.adapter.mapper;
